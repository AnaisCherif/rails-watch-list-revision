<h3>Add your review</h3>

<%= simple_form_for [@list, @review] do |f| %>
<%= f.input :comment %>
<%= f.input :rating, as: :select, prompt: 'Choose your rating', collection: ["0", "1", "2", "3", "4", "5"]  %>
<%= f.submit "Comment", class: "btn btn-ghost mt-3" %>
<% end %>
