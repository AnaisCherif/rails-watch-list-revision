<%= render "navbar" %>

<div class="container mt-3">
  <h1>Add a list</h1>

  <%= simple_form_for(@list) do |f| %>
  <%= f.input :name %>
  <%= f.submit "Submit", class: "btn btn-ghost mt-2" %>
  <% end %>

</div>
